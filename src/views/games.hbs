<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="keywords" content="бочча,счёт,спорт">
  <meta name="description" content="Страница с текущими играми по Бочча">
  {{#each styles}}
    <link href="{{cssPath}}" rel="stylesheet">
  {{/each}}
  {{#each scripts}}
    <script src="{{jsPath}}"></script>
  {{/each}}
  <title>Текущие игры</title>
</head>
<body>
{{> header}}
<main class="main">
  {{> games}}
</main>
{{> footer}}
</body>
</html>
